import { add } from "./sample-4-functions-1";
import { GeneratePESEL } from "./pesel";
GeneratePESEL();
console.log(add(Math.random(), Math.random()));

// Zadanie: Stwórz moduł udestępniający funkcję generującą pesel
